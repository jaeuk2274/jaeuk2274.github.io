daily_create

일근태 조회

IS_DC03 / 일근태
있으면 삭제

IS_DC04
근태생성 예외자
필터

직원정보 확인

is_dc07
교대근무조 형태 확인

    SELECT d.work_type
      INTO g_work_type
      FROM is_dc07 d
     WHERE d.emp_id = emp.emp_id
       AND d.dutydat = (SELECT MAX(d.dutydat)
                          FROM is_dc07 d
                         WHERE d.emp_id = emp.emp_id
                           AND d.dutydat <= p_dutydat
                           
                           
                           
g_work_type - 근무형태


                           
